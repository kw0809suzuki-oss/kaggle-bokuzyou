name: Relationship Surface Body v0 Runtime Smoke

on:
  push:
    branches:
      - "body/relationship-surface-v0-20260926"
    paths:
      - "run_relationship_surface_body_v0_runtime_smoke.py"
      - ".github/workflows/relationship-surface-body-v0-runtime-smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  runtime-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install official Kaggle environments
        run: python -m pip install -r requirements.txt
      - name: Fetch pinned Seyamalam opponent
        run: |
          mkdir -p opponents
          curl -fL --retry 3             -o opponents/seyamalam_v21.py             https://raw.githubusercontent.com/Seyamalam/Kaggriculture/8b8c421eb10634c756583ce10c75189f50c83a72/main.py
      - name: Run standalone Body twice on fresh World
        run: python run_relationship_surface_body_v0_runtime_smoke.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: relationship-surface-body-v0-runtime-smoke
          path: relationship_surface_body_v0_runtime_smoke.json
